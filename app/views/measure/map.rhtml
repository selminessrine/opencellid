<div id="map" style="width: 750px; height: 400px;"></div>
<script>
//var map = new GMap2(document.getElementById("map"));
//map.setCenter(new GLatLng(37.4419, -122.1419), 13);

var mapstraction = new Mapstraction('map','google');
var myPoint = new LatLonPoint(0,0);
mapstraction.setCenterAndZoom(myPoint, 2);
mapstraction.addLargeControls();
<% @mesures.each do |mesure| %>
var m=new  Marker(new LatLonPoint(<%= mesure.lat -%>,<%= mesure.lon -%>));
m.setInfoBubble('<%= mesure.created_at.to_s+"<br/>CellID:"+mesure.cell.id.to_s+"<br/>"+link_to("Cell", {:controller=>"cells", :action=>"show",:id=>mesure.cell.id }) -%>');
mapstraction.addMarker(m);
<% end %>
</script>
